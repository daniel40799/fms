name: Legacy Azure Static Web Apps Workflow Notice

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment moved
        run: echo "Use azure-dev.yml for develop -> dev and azure-prod.yml for main -> prod."
