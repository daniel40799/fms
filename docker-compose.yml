services:
  postgres:
    image: postgres:16
    container_name: fms-postgres
    environment:
      POSTGRES_DB: fms_dev
      POSTGRES_USER: fms
      POSTGRES_PASSWORD: fmspass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: